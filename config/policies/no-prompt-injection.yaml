id: no-prompt-injection
domain: prompt-security
regulation: EU-AI-Act
action: block
priority: 95
description: Block prompt-injection attempts before they reach the LLM.
triggers:
  classifier_labels: [prompt_injection]
